currently uberview fails for files that include git merge conflict markers
if a file includes conflict markers, they should be excluded for the normal parsing but added with location to uberview's output
