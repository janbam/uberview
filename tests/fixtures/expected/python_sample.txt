=== tests/fixtures/sample_project/src/python_sample.py ===

"""Python module docs."""
# Module context that should stay.
[5] Skipped top-level assignments/constants (1 item)
[8-21] Function: top_level
@cache
def top_level(
    value: int,
) -> int:
    """Handle the top-level case."""
    # Normalize before branching.

    [14-16] Function: nested
    def nested(value: int) -> int:
        # Leave the nested exit visible.


[24-27] Function: stream_names
def stream_names(names: list[str]):
    # Yield the normalized values.

[30-39] Class: Greeter
class Greeter:
    """Greeter docs."""

    [33-39] Method: load_many
    async def load_many(
        self,
        names: list[str],
    ) -> list[str]:
        """Format several names."""
        # Keep the final result visible.
