=== tests/fixtures/sample_project/src/python_sample.py ===

"""Python module docs."""
# Module context that should stay.
5-5	DEFAULT_NAME = "treebrief"
8-21	@cache
def top_level(
    value: int,
) -> int:
    """Handle the top-level case."""
    # Normalize before branching.
14-16	    def nested(value: int) -> int:
        # Leave the nested exit visible.
        return value + 1
        raise ValueError("negative")
    return nested(value)
24-27	def stream_names(names: list[str]):
    # Yield the normalized values.
        yield name.upper()
30-39	class Greeter:
    """Greeter docs."""
33-39	    async def load_many(
        self,
        names: list[str],
    ) -> list[str]:
        """Format several names."""
        # Keep the final result visible.
        return [name.upper() for name in names]
